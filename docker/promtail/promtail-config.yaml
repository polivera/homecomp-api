server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  # Production: Scrape Docker container logs
  # Uncomment and configure when deploying to production
  # - job_name: docker
  #   docker_sd_configs:
  #     - host: unix:///var/run/docker.sock
  #       refresh_interval: 5s
  #       filters:
  #         - name: label
  #           values: ["com.docker.compose.project=homecomp-api"]
  #   relabel_configs:
  #     - source_labels: ['__meta_docker_container_name']
  #       regex: '/(.*)'
  #       target_label: 'container'
  #     - source_labels: ['__meta_docker_container_log_stream']
  #       target_label: 'stream'
  #   pipeline_stages:
  #     - json:
  #         expressions:
  #           level: level
  #           message: message
  #           timestamp: time
  #     - labels:
  #         level:
  #         stream:
  #     - timestamp:
  #         source: timestamp
  #         format: RFC3339

  # Development: Optional system logs monitoring
  - job_name: system
    static_configs:
      - targets:
          - localhost
        labels:
          job: system
          __path__: /var/log/*log
